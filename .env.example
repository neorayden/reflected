# Primary: Groq for insight generation (https://console.groq.com/keys)
GROQ_API_KEY=gsk-...

# Fallback: Hugging Face if Groq fails (https://huggingface.co/settings/tokens)
HUGGINGFACE_API_KEY=hf_...

# Optional: OpenAI (only for Whisper transcription if not using local/server)
# OPENAI_API_KEY=sk-...

# Optional: Groq model (default: llama-3.1-70b-versatile)
# GROQ_MODEL=llama-3.1-8b-instant

# Optional: port (default 3000)
# PORT=3000
